css 
1) in the file
2) inline as a javascript  object //css in javascript
  style={styleCard} // not preferred. 

  const styleCard = {
    backgroundColor: "#f0f0f0";
  }

// never use indexes as keys, as everytime a new index will be added, if order of items in the list changes,